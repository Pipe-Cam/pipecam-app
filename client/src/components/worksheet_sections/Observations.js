import React from 'react'
import FormInputs from '../FormInputs'
import {observations} from '../../inputsMetadata'

function Observations() {
    // TODO: make sure the location address & access details are at the top (or on the side of) of each page when documenting observations
    // this is necessary information when dictating observations to the video.
    
    return (
        <div className="mt-3 pt-3 pb-2 px-4 border">
            <h3>Observations</h3>
            <div className="pt-2">
                
            </div>
        </div>
    )
}

export default Observations
